

{%% if not m.cluster.repository.archives.disabled %%}
- name: "Archives repo setup"
  import_playbook: "archives_repo.yml"
{%% endif %%}


{%% if not m.cluster.repository.apt_cacher.disabled %%}
- name: "Apt cacher setup"
  import_playbook: "apt_cacher_server.yml"
{%% endif %%}
